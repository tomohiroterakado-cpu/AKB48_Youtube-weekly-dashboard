{
  "name": "akb-weekly-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "AKBの素を出すちゃんねる weekly YouTube dashboard",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check director.js && node --check prepublish.js && node --check thumbnail-caption-layout.js && node --check thumbnail.js && node --check lib/channel-strategy.js && node --check lib/prepublish-review.js && node --check lib/images2-client.js && node --check lib/thumbnail-workflow.js && node --check data/latest.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
